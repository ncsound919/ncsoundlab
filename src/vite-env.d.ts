/// <reference types="vite/client" />

interface ImportMetaEnv {
  /**
   * No build-time environment variables are required. The app is free and
   * fully local-first; integrations are configured at runtime and stored
   * on-device.
   */
}
